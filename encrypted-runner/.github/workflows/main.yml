name: Encrypted Runner

on:
  workflow_dispatch:
    inputs:
      payload:
        description: "JSON payload with usuario/senha/otpExportData"
        required: false
        default: ""
      callback_url:
        description: "Callback URL to receive result JSON"
        required: false
        default: ""

jobs:
  get-eproc-session:
    runs-on: windows-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "requirements.txt"

      - name: Install dependencies
        run: uv pip install --system -r requirements.txt

      - name: Run get-eproc-session script
        env:
          HEADLESS: "false"
          PYTHONUTF8: "1"
          RAW_PAYLOAD: ${{ github.event.inputs.payload }}
          RAW_CALLBACK: ${{ github.event.inputs.callback_url }}
          EPROC_PRIVATE_KEY_PEM: ${{ secrets.EPROC_PRIVATE_KEY_PEM }}
        run: python scripts/get-session.py
